# Public environment variables (exposed to client)
PUBLIC_ENVIRONMENT=dev
PUBLIC_API_URL=http://localhost:8000
PUBLIC_STORAGE_URL=https://bf65a0cc4cafaf3adbfcd3feac29dd11.r2.cloudflarestorage.com

# Private build-time variables (NOT exposed to client, only used during SSR/build)
# This token is used for server-side rendering to fetch CMS content and other protected data
# Get this value from server/config/dev.json or prod.json -> admin_api_token field
ARKY_TOKEN=arky_dev_admin_token_2025
